const mathOperation = require("./calculator");

describe("Calculator test", () = > {
    test("adding 1 + 2 should return 3", () => {
        expect(mathOperation.sum(1, 2)).toBe(3);
    });
});

const mathOperation = require("./calculator");

describe("Calculator test", () = > {
    test("subtracting 1 + 2 should return 3", () => {
        expect(mathOperation.sum(1, 2)).toBe(3);
    });
});

const mathOperation = require("./calculator");

describe("Calculator test", () = > {
    test("adding 1 + 2 should return 3", () => {
        expect(mathOperation.sum(1, 2)).toBe(3);
    });
});